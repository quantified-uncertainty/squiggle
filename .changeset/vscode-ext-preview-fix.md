---
"vscode-squiggle": patch
---

Fix the blank preview pane (issue #3836, caused by an `import.meta` error in the bundle), fix the dependency-graph view rendering (page height and graph stylesheet), and upgrade the extension's dependencies.

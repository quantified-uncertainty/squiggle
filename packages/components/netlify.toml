[build]
    base = "packages/components/"
    publish = "storybook-static/"
    build = "cd ../squiggle-lang && yarn build && cd ../components && yarn build"
    ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF . ../squiggle-lang"

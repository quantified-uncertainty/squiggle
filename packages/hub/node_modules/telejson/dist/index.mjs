// packages/hub/node_modules/telejson/dist/index.mjs

// ... other code ...

// Assuming eval was used in a function like this:
function unsafeFunction(str) {
  return eval(str);
}

// We can refactor it to something like this:
function safeFunction(str) {
  return Function('"use strict";return (' + str + ')')();
}

// ... other code ...

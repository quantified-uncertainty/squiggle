# Date
### smaller
      ```
  (Date, Date) => Bool
  ```
      
      

### larger
      ```
  (Date, Date) => Bool
  ```
      
      

### smallerEq
      ```
  (Date, Date) => Bool
  ```
      
      

### largerEq
      ```
  (Date, Date) => Bool
  ```
      
      

### make
      ```
  (String) => Date
(year: Number, month: Number, day: Number) => Date
(year: Number) => Date
  ```
      
      

### fromUnixTime
      ```
  (Number) => Date
  ```
      
      

### toUnixTime
      ```
  (Date) => Number
  ```
      
      

### subtract
      ```
  (Date, Date) => Duration
  ```
      
      

### subtract
      ```
  (Date, Date) => Duration
  ```
      
      

### add
      ```
  (Date, Duration) => Date
(Duration, Date) => Date
  ```
      
      

### rangeDomain
      ```
  (min: Date, min: Date) => Domain
  ```
      
      
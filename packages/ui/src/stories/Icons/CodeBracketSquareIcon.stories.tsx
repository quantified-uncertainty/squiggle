import type { Meta, StoryObj } from "@storybook/react";

import { CodeBracketSquareIcon } from "../../index.js";

const meta = { component: CodeBracketSquareIcon } satisfies Meta<
  typeof CodeBracketSquareIcon
>;
export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {};

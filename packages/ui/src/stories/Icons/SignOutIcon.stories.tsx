import type { Meta, StoryObj } from "@storybook/react";

import { SignOutIcon } from "../../index.js";

const meta = { component: SignOutIcon } satisfies Meta<typeof SignOutIcon>;
export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {};

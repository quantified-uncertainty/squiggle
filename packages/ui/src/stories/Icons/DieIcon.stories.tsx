import type { Meta, StoryObj } from "@storybook/react";

import { Die6Icon } from "../../index.js";

const meta = { component: Die6Icon } satisfies Meta<typeof Die6Icon>;
export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {};

import type { Meta, StoryObj } from "@storybook/react";

import { FocusIcon } from "../../index.js";

const meta = { component: FocusIcon } satisfies Meta<typeof FocusIcon>;
export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {};

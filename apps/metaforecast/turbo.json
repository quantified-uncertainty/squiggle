{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["DATABASE_URL", "VERCEL_ENV"],
      "outputs": [
        "dist",
        "prisma/generated",
        "build",
        ".next",
        "*.tsbuildinfo",
        "!.next/cache"
      ]
    }
  }
}

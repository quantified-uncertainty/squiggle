[build]
    base = "./"

[build.environment]
    NETLIFY_USE_YARN = "true"

[context.deploy-preview]
    ignore = "node -e netlify-skip-deploy.js # 'process.env.BRANCH.includes(\"dependabot\") ? 0 : 1'"